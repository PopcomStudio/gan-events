{% extends 'admin/event/base.html.twig' %}

{% block form_content %}

    {{ form_start(formConfirm) }}
    <div class="row justify-content-center">
        <div class="col-12 col-md-6 col-lg-4">
            <div class="card">
                <div class="card-body text-center">
                    <p>{{ formTitle }}</p>
                    {{ form_widget(formConfirm.confirm) }}
                    <a href="{{ backLink }}" class="btn btn-primary">Non</a>
                </div>
            </div>
        </div>
    </div>
    {{ form_end(formConfirm) }}

{% endblock %}
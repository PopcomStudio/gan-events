<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Ticket</title>
    <style>
        @page {
            margin: 0;
        }

        body {
            font-family: OpenSans, sans-serif;
            color: {{ color }};
            font-weight: normal;
        }

        .arrow-wrapper {
            background: {{ secondary }};
            width: 70px;
            height: .75cm;
            position: absolute;
            right: 1cm;
            top: 0;
        }

        .arrow {
             position: absolute;
            top: .75cm;
            left: 0px;
            display : inline-block;
            height : 0;
            width : 0;
            border-top : 35px solid {{secondary}};
            border-right : 35px solid transparent;
            border-left : 35px solid transparent;
        
        }
    </style>
</head>
<body>
<table border="0" cellpadding="0" cellspacing="0" style="position:absolute;width:100%;height:100%;">
    <tr>
        <td style="width:10cm" valign="top">
            {% if guest.event.visual %}
                <img src="{{ visual }}" alt="" width="100%">
            {% endif %}
        </td>
        <td style="width:11cm" valign="top">
            <div class="arrow-wrapper"><div class="arrow"></div></div>
            <div style="margin:0.5cm;height:100%;">
                <div style="height:100%;background-color:{{ light }};position:relative;">
                    <div style="padding:0.5cm;">
                        <div style="text-transform:uppercase;margin-top:-.2cm;margin-bottom:.2cm;font-size:.8em">
                            Ticket
                        </div>
                        <div style="width:1cm;height:.07cm;background-color:{{ primary }};"></div>
                        <div style="font-family:OpenSansExtra;color:{{ primary }};font-size:1.5em;line-height:0.8;margin-top:0.3cm">
                            {{ event.name }}
                        </div>
                        <div>
                            {{ event.beginAt|format_datetime(pattern="eee d MMM y, H:mm", locale="fr")|capitalize }}
                        </div>
                        <div style="margin-top:1cm;font-weight:bold">
                            {{ guest.displayName }}
                        </div>
                        <div style="font-size:.8em;margin-top:-.1cm">{{ guest.email }}</div>
                        
                    </div>
                    <div style="position:absolute;bottom:1cm;right:.5cm;">
                        <img src="{{ qrcode }}" alt="" style="width:2cm;height:2cm">
                        </div>
                        <div style="font-size:.8em;margin-top:0cm;padding-left:0.5cm;padding-top: 0.8cm">
                        <p>A présenter lors de votre arrivée</p>
                        </div>
                    
                </div>
            </div>
        </td>
    </tr>
</table>
</body>
</html>